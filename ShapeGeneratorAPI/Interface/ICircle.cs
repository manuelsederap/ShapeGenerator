namespace ShapeGeneratorAPI.Interface
{
    public interface ICircle
    {
        double CalculateRadius(double diameter);
        double CalculateArea(double radius);
        double CalculateCircumference(double diameter);
    }
}
